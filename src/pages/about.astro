---
import Layout from '../layouts/Layout.astro';
import { load } from '../scripts/load';
const { translations: t } = await load(Astro);
---

<Layout title={t.about.page}>
  <h1>About</h1>
  <a href="/">Home</a>
</Layout>
