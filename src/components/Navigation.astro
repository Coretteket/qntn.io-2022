---
import Button from './Button.astro';
import ToggleLocale from './ToggleLocale.astro';
import ToggleTheme from './ToggleTheme.astro';
---

<nav>
  <div>
    <img src="/headshot.webp" alt="headshot" width={36} height={36} />
    <Button href="/">Quinten Coret</Button>
  </div>
  <div>
    <ToggleTheme />
    <ToggleLocale />
  </div>
</nav>

<style lang="scss">
  nav {
    display: flex;
    justify-content: space-between;
    margin-block: 3rem 2rem;
    margin-inline: auto;
    max-width: var(--container-width);
  }

  div {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  img {
    background-color: var(--btn-bg-color);
    color: transparent;
    border-radius: 50%;
  }

  a {
    text-decoration: none;
    padding: 0.75rem;
  }

  div > :global(a) {
    color: var(--txt-color);
    font-weight: 600;
  }
</style>
