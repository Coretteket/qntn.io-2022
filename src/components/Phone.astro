<div class="phone">
  <slot />
</div>

<style lang="scss">
  .phone {
    width: 100%;
    aspect-ratio: 1/2;
    background-color: var(--btn-bg-color);
    border-radius: 3rem;
    border: 1rem solid var(--project-accent);
    position: relative;
    padding-top: var(--top-height);

    --top-width: 35%;
    --top-height: 1rem;
  }

  .phone::after {
    content: '';
    width: var(--top-width);
    height: var(--top-height);
    background: var(--project-accent);
    border-radius: 0 0 1rem 1rem;
    margin-inline: auto;
    position: absolute;
    top: -1px;
    left: calc(50% - var(--top-width) / 2);
  }
</style>
